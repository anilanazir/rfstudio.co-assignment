# issue 1 PERFORMANCE 

* The major issue hurting the website is the performance issue . I have checked it through chrome devTools and the performance is very low(in 60s) ,website is taking forever to load which can be the reason for increased bounce rate ,as user leave immediatley when website is taking time in loading.
* i am seeing it almost everywhere on home page ,inner pages .
* the problem is mainly arising due to large images ,also all the images download before we can even reach to it .i will fix it by optimizing images and by applying lazy loading to all images as "lazy loading" is missing on rfstduio.co pages. Also plugins from wordpress adding extra css and js making website heavier.
Rankbetter image issue:
At rank better lazy loading is applied  but someone set the image width 3840 px on every card which is directly effecting the performance. Next.js handles images very well but the developer just given the wrong width. i will fix it by applying the actual css width needed. so that the images should be optimized.
Most importantly , the performance is worst at mobile view(almost 40%) due to heavy assets , larger fonts and such other problems.   

# issue 2 spacing issue
* there are a lot of spacing issues in the website. if fewer then these can be ignored but there are a lot almost on every page .
 *at Resource page the content and image is not alligned in hero section.
* at digital experience page , the problem section is not alligned.
* at rankbetter large gaps between sections on home page. also navlinks active status does not show ,when clicked on navlinks it does nothing . it shows hover below the first three links. 
* there is no continuity in padding and margins on most of the pages making website unimpressive.
* at growth diagnostic there is no gap between last section and footer.
*at events page in resources the hero section and nav have no space.
* Allignment issues can be fixed by using flex and uniform padding and margins as i used in footer.


# issue 3 FOOTER 
 * footer is overloaded with links ,Oakridge is not linked ,while others are linked. footer has too many navigation links but no CTA and no conversion purpose. 
 * it should have a "book a call CTA" or an email capture that attempts to keep the user . 
 *i am making an engaging and concise(Raw version)footer which will be better for website both in desktop and mobile view.

# issue 4 typography
*font sizes are much larger in mobile view. 
*there are enlarged logos in multiple inner pages. 
* hero section did not give a clear message at home page .it is vague and unable to explain to user what the company actually does.
*at Home page , rank better and growth image which are screenshots  not giving clear text. social icons are visible but not accessible, bullets points are not visible.

# weak CTA and Contact form issue

*There is weak CTA on home page of rfstudio .it's just get started almost everywhere, it can be anything else see how it work ,see our works or how it works . there is no continuity for CTA ,the layout for the contact form get started button if different on different pages .
* at resource page and about page ,nav get started button is going nowhere . contact form should be present in all pages with atleast 4 fields so that if the users want to contact then get started button should make them reach to contact form. 